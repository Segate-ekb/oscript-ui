// Пустой словарь интерфейса — реализация контракта «СловарьИнтерфейса» по умолчанию.
// Своих строк у кита нет и быть не может, поэтому словарь ПУСТ: инсталляция подставляет
// свой класс, объявив то же прозвище и пометив его &Верховным.
//
// Контракт живёт ПРОЗВИЩЕМ, а не именем класса: ОСень ищет желудь сперва по имени и лишь
// потом по прозвищу, поэтому класс, НАЗВАННЫЙ «СловарьИнтерфейса», перебить было бы
// нельзя ничем — до сравнения &Верховных дело не доходит.

Перем Тексты; // Соответствие: ключ (Строка) -> текст (Строка)

// Текст по ключу. Что делать с отсутствующим ключом, решает резолвер: словарь — данные.
//
// Параметры:
//  Ключ - Строка - ключ текста.
//
// Возвращаемое значение:
//  Строка, Неопределено - текст либо Неопределено, если ключа нет.
//
Функция НайтиТекст(Знач Ключ) Экспорт
	Возврат Тексты.Получить(Ключ);
КонецФункции

// Все ключи словаря.
//
// Возвращаемое значение:
//  Массив из Строка
//
Функция Ключи() Экспорт

	Результат = Новый Массив();
	Для Каждого КиЗ Из Тексты Цикл
		Результат.Добавить(КиЗ.Ключ);
	КонецЦикла;
	Возврат Результат;

КонецФункции

&Прозвище("СловарьИнтерфейса")
&Желудь
Процедура ПриСозданииОбъекта() Экспорт

	Тексты = Новый Соответствие();

КонецПроцедуры
